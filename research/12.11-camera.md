### 12.11. Camera / timelapse

Studio-built `camera.ipcam_*` MQTT commands. Plugin forwards only. Envelope: [§6.2](06.02-mqtt.md). Inbound `ipcam.*` / timelapse storage fields in `push_status`: [§12.1.5](12.01.05-fields-camera-ai.md). 
#### Live example (LAN P2S) — `ipcam_get_media_info`

```json
{
  "camera": {
    "command": "ipcam_get_media_info",
    "sequence_id": "20021",
    "sub_command": "is_timelapse_storage_enough",
    "storage": "internal",
    "total_layer": 1,
    "result": 0,
    "is_enough": true,
    "file_count": 166,
    "peer_host": "ipcam"
  }
}
```


#### Timelapse-storage preflight (`ipcam_get_media_info`)

Right before a print starts, when the user enabled timelapse and the printer reports `is_support_internal_timelapse`, Studio runs a storage preflight. The check is **driven by Studio, not by the plugin**: `SelectMachineDialog::start_timelapse_storage_check()` → `MachineObject::command_ipcam_check_timelapse_storage(storage, total_layer)` → `MachineObject::publish_json()`, which routes through `bambu_network_send_message_to_printer` (LAN) or `bambu_network_send_message` (cloud) with this opaque payload:

```json
{
  "camera": {
    "command":     "ipcam_get_media_info",
    "sub_command": "is_timelapse_storage_enough",
    "sequence_id": "20021",
    "storage":     "internal",
    "total_layer": 50
  }
}
```

The printer answers on the same channel with `result`, `is_enough`, `file_count`; Studio reads them in `DeviceManager.cpp:3548-3553` and either proceeds with `on_send_print()` or shows the "free up storage" dialog. From the plugin's perspective the frame is opaque — it is neither parsed nor formatted plugin-side, only forwarded byte-for-byte by the generic `send_message*` ABI calls.

Print-start ABI and plugin-built `project_file`: [§8.8](08.08-print-abi.md). Studio orchestration / when this runs relative to `start_*`: [§8.8.1](08.08-print-abi.md#881-common-contract).


#### Command builders (Studio)

#### `camera.ipcam_record_set`

Toggle recording while printing.

| name | type/values | notes |
|---|---|---|
| `sequence_id` | string | Studio counter; example fixed to `"20000"` |
| `command` | string | constant `"ipcam_record_set"` |
| `control` | string | `enable` or `disable` |

**Reply parsing:** `[`src/slic3r/GUI/DeviceManager.cpp:3613-3618`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/DeviceManager.cpp#L3613-L3618)`.

```json
{
  "camera": {
    "sequence_id": "20000",
    "command": "ipcam_record_set",
    "control": "enable"
  }
}
```

#### `camera.ipcam_timelapse`

Toggle timelapse capture.

| name | type/values | notes |
|---|---|---|
| `sequence_id` | string | Studio counter; example fixed to `"20000"` |
| `command` | string | constant `"ipcam_timelapse"` |
| `control` | string | `enable` or `disable` |

**Reply parsing:** `[`src/slic3r/GUI/DeviceManager.cpp:3603-3612`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/DeviceManager.cpp#L3603-L3612)`.

```json
{
  "camera": {
    "sequence_id": "20000",
    "command": "ipcam_timelapse",
    "control": "enable"
  }
}
```

#### `camera.ipcam_resolution_set`

Set camera resolution.

| name | type/values | notes |
|---|---|---|
| `sequence_id` | string | Studio counter; example fixed to `"20000"` |
| `command` | string | constant `"ipcam_resolution_set"` |
| `resolution` | string | printer-supported value |

**Reply parsing:** `[`src/slic3r/GUI/DeviceManager.cpp:3619-3627`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/DeviceManager.cpp#L3619-L3627)`.

```json
{
  "camera": {
    "sequence_id": "20000",
    "command": "ipcam_resolution_set",
    "resolution": "1080p"
  }
}
```

#### `camera.ipcam_get_media_info`

Check timelapse storage capacity.

| name | type/values | notes |
|---|---|---|
| `sequence_id` | string | Studio counter; example fixed to `"20000"` |
| `command` | string | constant `"ipcam_get_media_info"` |
| `sub_command` | string | `is_timelapse_storage_enough` |
| `storage` | string | storage target |
| `total_layer` | int | planned layers |

**Reply parsing:** `[`src/slic3r/GUI/DeviceManager.cpp:3628-3638`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/DeviceManager.cpp#L3628-L3638)`.

```json
{
  "camera": {
    "sequence_id": "20000",
    "command": "ipcam_get_media_info",
    "sub_command": "is_timelapse_storage_enough",
    "storage": "sdcard",
    "total_layer": 500
  }
}
```

#### `camera.ipcam_delete_oldest_timelapse`

Delete oldest timelapse media to make space.

| name | type/values | notes |
|---|---|---|
| `sequence_id` | string | Studio counter; example fixed to `"20000"` |
| `command` | string | constant `"ipcam_delete_oldest_timelapse"` |
| `storage` | string | storage target |
| `total_layer` | int | planned layers |

**Reply parsing:** No command-specific parser.

```json
{
  "camera": {
    "sequence_id": "20000",
    "command": "ipcam_delete_oldest_timelapse",
    "storage": "sdcard",
    "total_layer": 500
  }
}
```

#### `camera.ipcam_cap_pic_set`

Toggle snapshot capture.

| name | type/values | notes |
|---|---|---|
| `sequence_id` | string | Studio counter; example fixed to `"20000"` |
| `command` | string | constant `"ipcam_cap_pic_set"` |
| `control` | string | `enable` or `disable` |

**Reply parsing:** State/capability: `[`src/slic3r/GUI/DeviceCore/DevPrintOptions.cpp:227-230`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/DeviceCore/DevPrintOptions.cpp#L227-L230)`.

```json
{
  "camera": {
    "sequence_id": "20000",
    "command": "ipcam_cap_pic_set",
    "control": "enable"
  }
}
```


#### Live example (LAN P2S) — `ipcam_get_media_info`

```json
{
  "camera": {
    "command": "ipcam_get_media_info",
    "sequence_id": "20021",
    "sub_command": "is_timelapse_storage_enough",
    "storage": "internal",
    "total_layer": 1,
    "result": 0,
    "is_enough": true,
    "file_count": 166,
    "peer_host": "ipcam"
  }
}
```

Hub: [§12](12-mqtt.md).
