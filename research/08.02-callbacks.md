### 8.2. Callbacks (registration)

After `create_agent` and before `start()`, Studio installs a battery of `std::function` callbacks so the plugin can push events back into the GUI ([§8.1.1](08.01-initialization.md)). Callback typedefs live in [`bambu_networking.hpp`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/bambu_networking.hpp#L125-L151); setter typedefs / wrappers in [`NetworkAgent.hpp`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/NetworkAgent.hpp). Most setters are registered from [`GUI_App::init_networking_callbacks`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L2036-L2338); SSDP and HTTP-error callbacks are installed later in [`post_init`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L1362-L1378) / [`restart_networking`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L1938-L1960).

Per-call progress / cancel lambdas (`OnUpdateStatusFn`, `WasCancelledFn`, …) are **not** agent-level setters — they are arguments on bind / print / preset APIs ([§8.6](08.06-bind.md), [§8.8](08.08-print-abi.md), [§8.9](08.09-presets.md)).


| Symbol                                      | Setter signature                                      | Callback typedef                                                                 |
| ------------------------------------------- | ----------------------------------------------------- | -------------------------------------------------------------------------------- |
| `bambu_network_set_queue_on_main_fn`        | `int(void*, QueueOnMainFn)`                           | `QueueOnMainFn = std::function<void(std::function<void()>)>`                     |
| `bambu_network_set_on_ssdp_msg_fn`          | `int(void*, OnMsgArrivedFn)`                          | `OnMsgArrivedFn = std::function<void(std::string)>`                              |
| `bambu_network_set_on_http_error_fn`        | `int(void*, OnHttpErrorFn)`                           | `OnHttpErrorFn = std::function<void(unsigned, std::string)>`                     |
| `bambu_network_set_get_country_code_fn`     | `int(void*, GetCountryCodeFn)`                        | `GetCountryCodeFn = std::function<std::string()>`                                |
| `bambu_network_set_server_callback`         | `int(void*, OnServerErrFn)`                           | `OnServerErrFn = std::function<void(std::string url, int status)>`               |
| `bambu_network_set_on_server_connected_fn`  | `int(void*, OnServerConnectedFn)`                     | `OnServerConnectedFn = std::function<void(int return_code, int reason_code)>`    |
| `bambu_network_set_on_message_fn`           | `int(void*, OnMessageFn)`                             | `OnMessageFn = std::function<void(std::string dev_id, std::string msg)>`         |
| `bambu_network_set_on_user_message_fn`      | `int(void*, OnMessageFn)`                             | same `OnMessageFn`                                                               |
| `bambu_network_set_on_subscribe_failure_fn` | `int(void*, GetSubscribeFailureFn)`                   | `GetSubscribeFailureFn = std::function<void(std::string topic)>`                 |
| `bambu_network_set_on_user_login_fn`        | `int(void*, OnUserLoginFn)`                           | `OnUserLoginFn = std::function<void(int online_login, bool login)>`              |
| `bambu_network_set_on_printer_connected_fn` | `int(void*, OnPrinterConnectedFn)`                    | `OnPrinterConnectedFn = std::function<void(std::string topic_str)>`              |
| `bambu_network_set_on_local_connect_fn`     | `int(void*, OnLocalConnectedFn)`                      | `OnLocalConnectedFn = std::function<void(int, std::string, std::string)>`        |
| `bambu_network_set_on_local_message_fn`     | `int(void*, OnMessageFn)`                             | same `OnMessageFn`                                                               |


#### 8.2.1. Common contract

**Return convention.** Every setter returns `int`: `0` on success, or a `BAMBU_NETWORK_ERR_*` code ([§8.16](08.16-errors.md)) — typically `BAMBU_NETWORK_ERR_INVALID_HANDLE` when `agent` is null / unknown. Passing an empty `std::function` clears the previous callback (Studio does not do this on the happy path).

**Registration timeline.**

1. **`init_networking_callbacks`** ([GUI_App.cpp:2036-2338](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L2036-L2338)), called from `on_init_network` before `start()` ([§8.1.1](08.01-initialization.md)): `set_server_callback` → `set_on_server_connected_fn` → `set_on_printer_connected_fn` → `set_get_country_code_fn` → `set_on_subscribe_failure_fn` → `set_on_local_connect_fn` → `set_on_message_fn` → `set_on_user_message_fn` → `set_on_local_message_fn` → `set_queue_on_main_fn`.
2. **`post_init` / `restart_networking`** ([GUI_App.cpp:1362-1378](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L1362-L1378), [1938-1960](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L1938-L1960)): (re)install **only** `set_on_ssdp_msg_fn` and `set_on_http_error_fn`, then `start_discovery(true, false)`. Those two setters are **not** part of `init_networking_callbacks`.
3. **`set_on_user_login_fn`** is **commented out** in current Studio ([GUI_App.cpp:2041-2043](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L2041-L2043)) — the ABI symbol still exists and a compatible plugin must export it.

**Threading.** Most Studio lambdas already marshal UI work with `CallAfter` (or `wxQueueEvent` for HTTP errors). That does **not** replace `QueueOnMainFn`: the plugin must still use `queue_on_main` whenever *it* needs to touch wxWidgets / GUI state from a worker thread. `get_country_code` is a **synchronous** pull and may run on a plugin worker thread — keep it cheap and lock-safe.

#### 8.2.2. `bambu_network_set_queue_on_main_fn`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `fn` | `QueueOnMainFn` | `QueueOnMainFn` — marshal callbacks onto the UI thread. |

**Purpose.** Provide the only safe path for the plugin to run work on Studio's GUI thread.

**Studio.** Registers last inside `init_networking_callbacks` ([GUI_App.cpp:2333-2335](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L2333-L2335)): the lambda is `CallAfter(callback)`.

**Plugin behaviour.** Store the function and invoke it with any lambda that must run on the main/GUI thread (dialogs, wx object access, etc.). Do not call wx APIs directly from plugin worker threads.

**Return.** `0` on success; non-zero on invalid handle / failure.

#### 8.2.3. `bambu_network_set_on_ssdp_msg_fn`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `fn` | `OnMsgArrivedFn` | `OnMsgArrivedFn` — SSDP NOTIFY JSON ([§8.2.3](08.02-callbacks.md)). |

**Purpose.** Deliver SSDP / LAN discovery announcements as JSON strings.

**Studio.** Installed in [`post_init`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L1362-L1373) / [`restart_networking`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L1945-L1955) (not in `init_networking_callbacks`). The λ `CallAfter`s into `DeviceManager::on_machine_alive(json_str)`. Expected keys and SSDP wire: [§6.1](06.01-ssdp.md). Discovery itself is started with `start_discovery` ([§8.4.2](08.04-lan.md)).

**Plugin behaviour.** When a printer is discovered / refreshed, call the stored callback with a JSON object Studio can parse (`dev_id`, `dev_ip`, …). May fire from a discovery thread — Studio already marshals to the GUI.

**Return.** `0` on success; non-zero on failure.

#### 8.2.4. `bambu_network_set_on_http_error_fn`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `fn` | `OnHttpErrorFn` | `OnHttpErrorFn` — non-success cloud HTTP ([§8.2.4](08.02-callbacks.md)). |

**Purpose.** Report HTTP failures from plugin-originated cloud requests to Studio.

**Studio.** Installed alongside SSDP in `post_init` / `restart_networking` ([GUI_App.cpp:1375-1377](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L1375-L1377)). Handler [`handle_http_error`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L5209-L5216) posts `EVT_HTTP_ERROR` via `wxQueueEvent` (no `CallAfter` in the registered λ).

**Plugin behaviour.** On transport / HTTP errors that Studio should surface globally, invoke with `(http_code, body)`. Keep `body` reasonably small (Studio comments that tips body must be under 1024 bytes).

Evidence: stock `02.08.01.53` via `gap_probe` / `query_bind` when `GET /v1/iot-service/api/user/bind_list?dev_ids=…` returned upstream **502** (APISIX/openresty HTML body): plugin fired `on_http_error(502, "<html>…502 Bad Gateway…")` and still returned `query_bind_status` with `http_code=502` / the same body. So at least cloud REST failures that surface a non-success HTTP status on bind-status use this callback. `set_server_callback` did **not** fire for the same 502.

**Return.** `0` on success; non-zero on failure.

#### 8.2.5. `bambu_network_set_get_country_code_fn`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `fn` | `GetCountryCodeFn` | `GetCountryCodeFn` — Studio pulls region on demand. |

**Purpose.** Let the plugin pull the current regional country code from Studio (sync).

**Studio.** [`init_networking_callbacks`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L2159-L2164): returns `app_config->get_country_code()` or `""`.

**Plugin behaviour.** Store the function; call it when resolving regional hosts if the agent needs a live value (in addition to `set_country_code` from init — [§8.1.9](08.01-initialization.md)). Must be safe on a worker thread.

**Return.** `0` on success; non-zero on failure. The callback itself returns `std::string`.

#### 8.2.6. `bambu_network_set_server_callback`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `fn` | `OnServerErrFn` | `OnServerErrFn` — generic cloud error channel. |

**Purpose.** Notify Studio of a generic server / network error that should show the network-error dialog.

**Studio.** First registration in `init_networking_callbacks` ([GUI_App.cpp:2045-2068](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L2045-L2068)). The λ captures `url` / `status` but the UI path only `CallAfter`s and shows `NetworkErrorDialog` (with suppress-if-already-shown / select-machine-dialog guards) — args are unused in the dialog body.

**Plugin behaviour.** Store and fire when a server-side failure warrants the global error dialog.

**Return.** `0` on success; non-zero on failure.

**TODO**: need capture of stock triggers for `set_server_callback` (when / with which `url` / `status`).

#### 8.2.7. `bambu_network_set_on_server_connected_fn`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `fn` | `OnServerConnectedFn` | `OnServerConnectedFn` — cloud MQTT connect result. |

**Purpose.** Report cloud MQTT broker connect / disconnect outcomes.

**Studio.** ([GUI_App.cpp:2072-2129](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L2072-L2129)):
- `return_code == 5` → logout + “Login information expired” dialog.
- `return_code < 0` → homepage MQTT-fail flag + one-shot connect-failed dialog.
- otherwise (`≥ 0`) → clear fail flag, `set_user_selected_machine`, optional multi-machine event, `TryLoadFromMqttCB`, resubscribe cache.

**Plugin behaviour.** Invoke after cloud MQTT `CONNACK` / fatal disconnect with the stock-style `(return_code, reason_code)` pair. Cloud MQTT ABI: [`connect_server`](08.03-cloud-mqtt.md) ([§8.3.3](08.03-cloud-mqtt.md)); sequence in [§8.3.1](08.03-cloud-mqtt.md).

Evidence: `gap_probe` vs stock `02.08.01.53`:

| Outcome | `(return_code, reason_code)` | Notes |
|---------|------------------------------|--------|
| Successful cloud CONNECT | `(0, 0)` | `is_server_connected() == true` |
| Failed connect (missing slicer cert / TLS broken) | `(-1, 255)` | Studio treats `return_code < 0` as MQTT-fail dialog path |

Studio source still documents `return_code == 5` as login-expired (forces logout). That value was **not** reproduced in this probe (would need an expired token on a TLS-healthy path).

**Return.** `0` on success; non-zero on failure.

#### 8.2.8. `bambu_network_set_on_message_fn`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `fn` | `OnMessageFn` | `OnMessageFn` — cloud device report frames. |

**Purpose.** Deliver cloud MQTT device report / push payloads for a `dev_id`.

**Studio.** ([GUI_App.cpp:2243-2279](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L2243-L2279)): `CallAfter` → optional `process_network_msg`, else `MachineObject::parse_json("cloud", msg)` (full parse for selected machine, lighter for others), AMS sidebar / filament sync, `update_machine_sync_status`.

**Plugin behaviour.** For each cloud device topic message, call with `(dev_id, msg)` where `msg` is the JSON payload Studio expects. Distinct from the LAN message callback (§8.2.14).

**Return.** `0` on success; non-zero on failure.

#### 8.2.9. `bambu_network_set_on_user_message_fn`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `fn` | `OnMessageFn` | `OnMessageFn` — cloud user-channel frames. |

**Purpose.** Deliver cloud MQTT payloads on the **user** channel (not a device topic).

**Studio.** ([GUI_App.cpp:2281-2297](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L2281-L2297)): `CallAfter`; if `user_id == m_agent->get_user_id()` → `UserManager::parse_json(msg)`.

**Plugin behaviour.** Invoke with `(user_id, msg)` for user-scoped MQTT traffic. Filter ownership is Studio-side; the plugin should still pass the correct `user_id`.

**Return.** `0` on success; non-zero on failure.

#### 8.2.10. `bambu_network_set_on_subscribe_failure_fn`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `fn` | `GetSubscribeFailureFn` | `GetSubscribeFailureFn` — rejected MQTT subscribe. |

**Purpose.** Report that a device subscription failed so Studio can retry.

**Studio.** ([GUI_App.cpp:2166-2169](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L2166-L2169)): `CallAfter` → `on_start_subscribe_again(dev_id)`. The typedef parameter is named `topic`, but Studio treats the string as a **`dev_id`**.

**Plugin behaviour.** On subscribe failure for a device, call with that device id (not a full MQTT topic string).

**Return.** `0` on success; non-zero on failure.

#### 8.2.11. `bambu_network_set_on_user_login_fn`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `fn` | `OnUserLoginFn` | `OnUserLoginFn` — installed but unused by stock Studio. |

**Purpose.** Notify Studio when the plugin detects a user login / logout transition.

**Studio.** **Not registered** on the current pin — the call is commented out ([GUI_App.cpp:2041-2043](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L2041-L2043)). Previously it would have called `request_user_handle(online_login)`.

**Plugin behaviour.** Export the setter and accept / store the callback for ABI completeness. Firing it has no effect until Studio re-enables registration. Auth flows today go through the auth ABI ([§8.5](08.05-auth.md) / [§8.5.1](08.05-auth.md)) and GUI login events, not this callback.

**Return.** `0` on success; non-zero on failure.

#### 8.2.12. `bambu_network_set_on_printer_connected_fn`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `fn` | `OnPrinterConnectedFn` | `OnPrinterConnectedFn` — cloud/tunnel device online. |

**Purpose.** Signal that a printer MQTT session (LAN or tunnel) is up and ready for push / cert install.

**Studio.** ([GUI_App.cpp:2131-2157](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L2131-L2157)): `CallAfter`; optional `tunnel/` prefix on `topic_str` sets `is_tunnel_mqtt`; then `command_request_push_all`, `command_get_version`, access-code refresh, `install_device_cert`.

**Plugin behaviour.** After a successful printer MQTT connect, invoke with `topic_str` equal to `dev_id`, or `"tunnel/" + dev_id` for the tunnel path.

**Return.** `0` on success; non-zero on failure.

#### 8.2.13. `bambu_network_set_on_local_connect_fn`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `fn` | `OnLocalConnectedFn` | `OnLocalConnectedFn` — LAN MQTT session state. |

**Purpose.** Report LAN MQTT session state changes (`ConnectStatus`).

**Studio.** ([GUI_App.cpp:2172-2241](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L2172-L2241)): `CallAfter`; branches on `ConnectStatusOk` / `Failed` / `Lost` for LAN-mode vs cloud-paired machines (push_all, erase machine, incorrect-password when `msg == "5"`, `EVT_CONNECT_LAN_MODE_PRINT`).

`ConnectStatus` ([bambu_networking.hpp:181-185](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/bambu_networking.hpp#L181-L185)):

```cpp
enum ConnectStatus {
    ConnectStatusOk = 0,
    ConnectStatusFailed = 1,
    ConnectStatusLost = 2,
};
```

**Plugin behaviour.** Call with `(status, dev_id, msg)` on LAN connect success / failure / loss. `msg == "5"` is treated by Studio as an incorrect access-code signal. LAN connect ABI: [`connect_printer`](08.04-lan.md) ([§8.4.3](08.04-lan.md)).

**Return.** `0` on success; non-zero on failure.

#### 8.2.14. `bambu_network_set_on_local_message_fn`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `fn` | `OnMessageFn` | `OnMessageFn` — LAN MQTT report frames. |

**Purpose.** Deliver LAN MQTT device report payloads (same `OnMessageFn` shape as cloud device messages).

**Studio.** ([GUI_App.cpp:2300-2332](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L2300-L2332)): `CallAfter` → optional `process_network_msg`, else `parse_json("lan", msg)`, AMS / filament sync for the selected machine, `update_machine_sync_status`.

**Plugin behaviour.** For each LAN device MQTT message, call with `(dev_id, msg)`. Do not route LAN traffic through `set_on_message_fn` (§8.2.8) — Studio distinguishes `"cloud"` vs `"lan"` parse paths.

**Return.** `0` on success; non-zero on failure.

Hub: [§8](08-abi-main.md).
