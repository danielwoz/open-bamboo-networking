### 9.2. C ABI surface (`Bambu_*`)

Pure-C tunnel API exported from `libBambuSource` / `BambuSource.dll`. Header: [BambuTunnel.h](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/Printer/BambuTunnel.h) (pin `12f17b06`). Studio uses the **dlopen** path (`#define BAMBU_DYNAMIC`): function pointers live in `BambuLib`, filled by `StaticBambuLib::get` ([PrinterFileSystem.cpp:1840-1879](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/Printer/PrinterFileSystem.cpp#L1840-L1879)). `PrinterFileSystem` and current `wxMediaCtrl3` inherit / embed that table.

Neighbors: load order ([§9.1](09.01-loading.md)); URL grammar ([§9.3](09.03-urls.md)); camera back-ends ([§9.4](09.04-camera-backends.md)); CTRL JSON ([§9.5](09.05-ctrl.md)); `:6000` wire ([§6.4](06.04-port-6000.md)); hub ([§9](09-abi-bambusource.md)).


| Symbol | Signature | Notes |
| --- | --- | --- |
| `Bambu_Init` | `int()` | Global init; rarely called |
| `Bambu_Deinit` | `void()` | Global teardown (`StaticBambuLib::release`) |
| `Bambu_Create` | `int(Bambu_Tunnel*, char const* path)` | Allocate tunnel from URL |
| `Bambu_Destroy` | `void(Bambu_Tunnel)` | Free tunnel |
| `Bambu_SetLogger` | `void(Bambu_Tunnel, Logger, void*)` | Log callback |
| `Bambu_SetStreamInfoCallback` | `void(Bambu_Tunnel, StreamInfoCallback, void*)` | Optional callback; `GET_FUNC`’d |
| `Bambu_SetTrackReporter` | `void(Bambu_Tunnel, TrackReporter, void*)` | Optional analytics; `GET_FUNC`’d |
| `Bambu_Open` | `int(Bambu_Tunnel)` | Begin connect; may `would_block` |
| `Bambu_Close` | `void(Bambu_Tunnel)` | Tear down session |
| `Bambu_StartStream` | `int(Bambu_Tunnel, bool video)` | Legacy start (camera / CTRL fallback) |
| `Bambu_StartStreamEx` | `int(Bambu_Tunnel, int type)` | Preferred; `0x3001` = CTRL |
| `Bambu_GetStreamCount` | `int(Bambu_Tunnel)` | Camera track count |
| `Bambu_GetStreamInfo` | `int(Bambu_Tunnel, int index, Bambu_StreamInfo*)` | Camera track metadata |
| `Bambu_GetDuration` | `unsigned long(Bambu_Tunnel)` | Declared; not exercised on live LAN |
| `Bambu_Seek` | `int(Bambu_Tunnel, unsigned long time)` | Declared; not exercised on live LAN |
| `Bambu_ReadSample` | `int(Bambu_Tunnel, Bambu_Sample*)` | Camera AU **or** CTRL response |
| `Bambu_SendMessage` | `int(Bambu_Tunnel, int ctrl, char const* data, int len)` | CTRL request |
| `Bambu_RecvMessage` | `int(Bambu_Tunnel, int* ctrl, char* data, int* len)` | Declared; Studio unused |
| `Bambu_GetSessionStat` | `void(Bambu_Tunnel, Bambu_SessionStat*)` | Optional metrics; `GET_FUNC`’d |
| `Bambu_GetLastErrorMsg` | `char const*()` | Error string fallback |
| `Bambu_FreeLogMsg` | `void(tchar const* msg)` | Free logger message |


#### 9.2.1. Common contract

**Wiring.** `StaticBambuLib` `GET_FUNC`s ([PrinterFileSystem.cpp:1856-1871](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/Printer/PrinterFileSystem.cpp#L1856-L1871)): `Create`, `Open`, `StartStream`, `StartStreamEx`, `GetStreamCount`, `GetStreamInfo`, `SendMessage`, `ReadSample`, `Close`, `Destroy`, `SetLogger`, `SetStreamInfoCallback`, `SetTrackReporter`, `GetSessionStat`, `FreeLogMsg`, `Deinit`. If `Bambu_Create` is missing → `Fake_Bambu_Create` returns `−2` (“library missing”). Header-only relative to this loader (still exported by stock dylibs): `Init`, `GetDuration`, `Seek`, `RecvMessage`, `GetLastErrorMsg`.

**Types** ([BambuTunnel.h:36-136](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/Printer/BambuTunnel.h#L36-L136)):

| Type | Role |
| --- | --- |
| `Bambu_Tunnel` | Opaque `void*` handle |
| `tchar` | `wchar_t` on Windows, `char` elsewhere — logger / `FreeLogMsg` |
| `Logger` | `void(*)(void* ctx, int level, tchar const* msg)` |
| `Bambu_Sample` | `{ itrack, size, flags, buffer, decode_time }` — one media AU or one CTRL envelope |
| `Bambu_StreamInfo` | Track type / subtype / format union + `format_buffer` |
| `Bambu_SessionStat` | Session metrics struct (optional API) |
| `PlayerEventC` / `TrackReporter` | LiveView analytics hook (optional API) |

**`Bambu_Error`:**

| Value | Enumerator | Meaning |
| ---: | --- | --- |
| `0` | `Bambu_success` | OK / sample ready |
| `1` | `Bambu_stream_end` | End of stream |
| `2` | `Bambu_would_block` | Not ready — poll again |
| `3` | `Bambu_buffer_limit` | Caller buffer too small |

Negative returns are **fatal**: Studio calls `Close` + `Destroy` and reconnects ([§9 hub](09-abi-bambusource.md#lifetime-error-propagation-and-reconnect)).

**Consumers.**

| Path | Lifecycle sketch |
| --- | --- |
| Camera (`wxMediaCtrl3` / `gstbambusrc`) | `Create(url)` → `SetLogger` → `Open` → `StartStream(true)` → `GetStreamInfo` → loop `ReadSample` |
| File browser (`PrinterFileSystem`) | `Create(url)` → `Open` → `StartStreamEx(0x3001)` → `SendMessage` / `ReadSample` JSON ([§9.5](09.05-ctrl.md)) |

**ABI note.** `extern "C"` with C structs by pointer/value — match Studio’s C ABI / calling convention. Safer than `bambu_networking` (no `std::*`), but `tchar` width still matters.


#### 9.2.2. `Bambu_Init`

**Parameters.** None.

**Purpose.** One-shot global library init.

**Studio.** Declared; not in the usual `StaticBambuLib` bring-up path. Observed shipping libs often no-op.

**Plugin behaviour.** Idempotent init if required; otherwise return success.

**Return.** `0` on success; negative on failure.


#### 9.2.3. `Bambu_Deinit`

**Parameters.** None.

**Purpose.** Global teardown.

**Studio.** `StaticBambuLib::release()` on agent reset.

**Plugin behaviour.** Release global resources; safe if never inited.

**Return.** `void`.


#### 9.2.4. `Bambu_Create`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `tunnel` | `Bambu_Tunnel*` | out: opaque handle |
| `path` | `char const*` | URL from Studio ([§9.3](09.03-urls.md)) |

**Purpose.** Allocate a tunnel bound to `path` (not necessarily connected).

**Studio.** Every camera / file-browser session. Missing export → `Fake_Bambu_Create` → `−2`.

**Plugin behaviour.** Parse URL; allocate handle. Connection happens in `Open` / `StartStream*`.

**Return.** `0` and non-null `*tunnel` on success; `−2` if library stub; other negatives fatal.


#### 9.2.5. `Bambu_Destroy`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `tunnel` | `Bambu_Tunnel` | Handle from `Create` |

**Purpose.** Free the tunnel after `Close`.

**Studio.** End of tab / reconnect teardown.

**Plugin behaviour.** Free all resources; must be safe after `Close` even if a reader was recently in `ReadSample`.

**Return.** `void`.


#### 9.2.6. `Bambu_SetLogger`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `tunnel` | `Bambu_Tunnel` | Tunnel handle |
| `logger` | `Logger` | Callback |
| `context` | `void*` | Opaque user pointer |

**Purpose.** Install a log sink for the tunnel.

**Studio.** Camera (`wxMediaCtrl3` / `wxMediaCtrl2.mm`) and file browser (`DumpLog`).

**Plugin behaviour.** Invoke from any thread with `tchar` messages; Studio frees via `FreeLogMsg`.

**Return.** `void`.


#### 9.2.7. `Bambu_SetStreamInfoCallback`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `tunnel` | `Bambu_Tunnel` | Tunnel handle |
| `callback` | `StreamInfoCallback` | `void(*)(void*, Bambu_StreamInfo*)` |
| `context` | `void*` | Opaque user pointer |

**Purpose.** Async stream-info notification (header API).

**Studio.** Resolved by `StaticBambuLib`; call sites are optional / sparse — safe no-op if Studio never registers a callback.

**Plugin behaviour.** Store callback; invoke when stream metadata is known, or no-op if unset.

**Return.** `void`.


#### 9.2.8. `Bambu_SetTrackReporter`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `tunnel` | `Bambu_Tunnel` | Tunnel handle |
| `reporter` | `TrackReporter` | `void(*)(void*, PlayerEventC const*)` |
| `context` | `void*` | Opaque user pointer |

**Purpose.** LiveView analytics events into Studio’s track pipeline (header API).

**Studio.** Resolved by `StaticBambuLib`; used for LiveView analytics when Studio installs a reporter. Related Studio tracking: [§8.13](08.13-tracking.md).

**Plugin behaviour.** Store reporter; emit `PlayerEventC` events or no-op if unset.

**Return.** `void`.


#### 9.2.9. `Bambu_Open`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `tunnel` | `Bambu_Tunnel` | Tunnel handle |

**Purpose.** Begin transport connect (TLS `:6000`, RTSP(S), TUTK, … per URL).

**Studio.** Polled until not `would_block` (file browser / camera).

**Plugin behaviour.** For LAN CTRL / MJPEG file URLs: TLS connect ([§6.4](06.04-port-6000.md)); CTRL login/setup follows in `StartStreamEx`. May return `Bambu_would_block` while connecting.

**Return.** `Bambu_success`, `Bambu_would_block`, or negative fatal.


#### 9.2.10. `Bambu_Close`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `tunnel` | `Bambu_Tunnel` | Tunnel handle |

**Purpose.** Close the active session; unblock any `ReadSample` waiter.

**Studio.** Tab close / fatal error / reconnect.

**Plugin behaviour.** Shutdown socket/relay; return only when destroy-safe.

**Return.** `void`.


#### 9.2.11. `Bambu_StartStream`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `tunnel` | `Bambu_Tunnel` | Tunnel handle |
| `video` | `bool` | `true` = video camera path; `false` = non-video / legacy CTRL fallback |

**Purpose.** Legacy stream start.

**Studio.** Camera `wxMediaCtrl3` / `gstbambusrc` with `video=true`. File browser falls back here when `StartStreamEx` is missing (`PrinterFileSystem.cpp:1747-1748`).

**Plugin behaviour.** Start media pipeline or legacy CTRL setup; may `would_block`.

**Return.** `Bambu_success`, `Bambu_would_block`, or negative.


#### 9.2.12. `Bambu_StartStreamEx`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `tunnel` | `Bambu_Tunnel` | Tunnel handle |
| `type` | `int` | Stream / mode selector. File browser: `CTRL_TYPE = 0x3001` ([PrinterFileSystem.h:32](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/Printer/PrinterFileSystem.h#L32)). |

**Purpose.** Preferred start; `0x3001` switches the tunnel to CTRL JSON-RPC ([§9.5](09.05-ctrl.md)).

**Studio.** File browser polls until success / timeout (~5 s).

**Plugin behaviour.** For `0x3001` on LAN: login + `mtype` 12291 setup on `:6000` ([§6.4](06.04-port-6000.md)), then accept framed CTRL JSON.

**Return.** `Bambu_success`, `Bambu_would_block`, or negative.


#### 9.2.13. `Bambu_GetStreamCount`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `tunnel` | `Bambu_Tunnel` | Tunnel handle |

**Purpose.** Number of media tracks after stream start.

**Studio.** Camera path after `StartStream`.

**Plugin behaviour.** Return track count (often `1` for video-only).

**Return.** Non-negative count; negative fatal.


#### 9.2.14. `Bambu_GetStreamInfo`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `tunnel` | `Bambu_Tunnel` | Tunnel handle |
| `index` | `int` | Track index |
| `info` | `Bambu_StreamInfo*` | out: track metadata |

**Purpose.** Describe a track (codec / resolution / format buffer).

**Studio.** Camera decoder setup (`AVVideoDecoder` / GStreamer caps).

**Plugin behaviour.** Fill `info` (including `format_buffer` / `format_size` when needed). Lifetime of `format_buffer` must outlive the call as Studio expects.

**Return.** `0` on success; negative on failure.


#### 9.2.15. `Bambu_GetDuration`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `tunnel` | `Bambu_Tunnel` | Tunnel handle |

**Purpose.** Stream duration (ms / ticks — header does not document units further).

**Studio.** **Not exercised** on live LAN camera / file browser.

**Plugin behaviour.** Return `0` if unknown.

**Return.** `unsigned long` duration.


#### 9.2.16. `Bambu_Seek`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `tunnel` | `Bambu_Tunnel` | Tunnel handle |
| `time` | `unsigned long` | Seek target |

**Purpose.** Seek in a recorded / VOD-style stream.

**Studio.** **Not exercised** on live LAN streams.

**Plugin behaviour.** Return error / no-op for live tunnels.

**Return.** `0` on success; negative on failure.


#### 9.2.17. `Bambu_ReadSample`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `tunnel` | `Bambu_Tunnel` | Tunnel handle |
| `sample` | `Bambu_Sample*` | out: one access unit or CTRL envelope |

**Purpose.** Non-blocking (or poll-style) read of the next sample.

**Studio.** Camera: MJPEG / H.264 AU. File browser: JSON [`\n\n` binary] response ([§9.5](09.05-ctrl.md)).

**Plugin behaviour.** Fill `sample->buffer` / `size` / `flags` / `itrack` / `decode_time`. Return `would_block` when empty; do not block forever.

**Return.** `Bambu_success`, `would_block`, `stream_end`, `buffer_limit`, or negative.


#### 9.2.18. `Bambu_SendMessage`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `tunnel` | `Bambu_Tunnel` | Tunnel handle |
| `ctrl` | `int` | Control channel; Studio passes `CTRL_TYPE` (`0x3001`) |
| `data` | `char const*` | CTRL JSON (+ optional `\n\n` binary) ([§9.5](09.05-ctrl.md)) |
| `len` | `int` | Byte length of `data` |

**Purpose.** Send a CTRL request on a file-browser tunnel.

**Studio.** `PrinterFileSystem::SendRequest` → worker queue → this call.

**Plugin behaviour.** Frame onto `:6000` / TUTK wire ([§6.4](06.04-port-6000.md)); match replies by `sequence`.

**Return.** `0` on accept; negative on failure.


#### 9.2.19. `Bambu_RecvMessage`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `tunnel` | `Bambu_Tunnel` | Tunnel handle |
| `ctrl` | `int*` | out: control id |
| `data` | `char*` | out: payload buffer |
| `len` | `int*` | in/out: buffer size / received length |

**Purpose.** Alternate message receive API.

**Studio.** **Declared but not called** for camera or file browser — responses use `ReadSample`.

**Plugin behaviour.** Optional; may return “not supported”.

**Return.** `0` on success; negative on failure.


#### 9.2.20. `Bambu_GetSessionStat`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `tunnel` | `Bambu_Tunnel` | Tunnel handle |
| `stat` | `Bambu_SessionStat*` | out: metrics |

**Purpose.** Query session statistics (header API).

**Studio.** Resolved by `StaticBambuLib`; call sites optional.

**Plugin behaviour.** Zero-fill or fill available metrics; safe no-op body is fine if Studio never queries.

**Return.** `void`.


#### 9.2.21. `Bambu_GetLastErrorMsg`

**Parameters.** None.

**Purpose.** Human-readable last error string.

**Studio.** Error-reporting fallback when present.

**Plugin behaviour.** Return a stable C string or empty; lifetime until next error overwrite.

**Return.** `char const*` (may be empty).


#### 9.2.22. `Bambu_FreeLogMsg`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `msg` | `tchar const*` | Message previously passed to `Logger` |

**Purpose.** Free a logger message allocated by the plugin.

**Studio.** After every log callback delivery.

**Plugin behaviour.** Free `msg`; no-op on null.

**Return.** `void`.

Hub: [§9](09-abi-bambusource.md).
