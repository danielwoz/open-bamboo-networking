### 12.1.3. `push_status` fields — AMS / filament

Leaf catalogue for AMS telemetry in `print.push_status`. Control commands: [§12.7](12.07-ams-filament.md). PA commands / storage model: [§12.8](12.08-pa-calibration.md).

Catalogue from a full (`msg: 0`) **P2S** frame; other models may omit keys. Studio pin: `12f17b06f4f537f9c03162d08bb70cf733c42839`. Parsers: `DevFilaSystem.cpp`, `DevExtruderSystem.cpp`.

#### Top-level AMS-related keys

| Field | Type | Meaning |
| --- | --- | --- |
| `ams` | object | AMS subsystem block (below) |
| `ams_status` | int | AMS FSM status code — Studio (`ams_status_main` derived) |
| `ams_rfid_status` | int | RFID subsystem status |
| `hw_switch_state` | int | Filament hub / PTFE switch hardware state — Studio |
| `cali_version` | int | PA storage generation (`0` = modern table; absent = legacy) — [§12.8](12.08-pa-calibration.md) |
| `vir_slot` | array | Virtual / external spool slot objects (same tray shape as AMS trays; `id` `"254"` / `"255"`) |

#### `ams` root

| Field | Type | Meaning |
| --- | --- | --- |
| `ams.ams[]` | array | Per-unit AMS objects |
| `ams.ams_exist_bits` | string | Hex bitfield: which AMS units exist |
| `ams.ams_exist_bits_raw` | string | Raw exist bits — **unknown** vs `ams_exist_bits` |
| `ams.tray_exist_bits` | string | Hex bitfield: physical spool present per slot — Studio presence source |
| `ams.tray_reading_bits` | string | Hex: RFID read in progress |
| `ams.tray_read_done_bits` | string | Hex: RFID read complete |
| `ams.tray_is_bbl_bits` | string | Hex: official Bambu RFID profile recognised |
| `ams.tray_hall_out_bits` | string | Hex: hall-sensor / “out” bits — **unknown** detail |
| `ams.tray_now` | string | Active flat tray id — Studio / `DevExtruderSystem` |
| `ams.tray_tar` | string | Target tray id during change |
| `ams.tray_pre` | string | Previous tray id |
| `ams.insert_flag` | bool | Read RFID on insert — Studio (`ams_user_setting`) |
| `ams.power_on_flag` | bool | Read RFID at power-on — Studio |
| `ams.calibrate_remain_flag` | bool | Remain-calibration enabled (when present) |
| `ams.cali_id` | int | Last calibration target tray id (`255` = external / none) |
| `ams.cali_stat` | int | Calibration status; low byte `0x01`–`0x04` = setup in progress (`DevFilaSystem::IsAmsSettingUp`) |
| `ams.unbind_ams_stat` | int | Unbind / unlink AMS state — **unknown** |
| `ams.version` | int | AMS protocol / structure version |
| `ams.cfs` | array | CFS / related units — **unknown** when non-empty (often `[]`) |

#### Per-unit `ams.ams[]`

| Field | Type | Meaning |
| --- | --- | --- |
| `id` | string | AMS unit id (`"0"`, …) |
| `humidity` | string | Humidity level (Studio parses as int level) |
| `humidity_raw` | string | Humidity percent / raw sensor — Studio `m_humidity_percent` |
| `temp` | string | AMS internal temperature °C |
| `info` | string | Packed info / capability word — **unknown** encoding |
| `dry_time` | int | Remaining drying time |
| `dry_sf_reason` | array | Soft-fail reasons for drying |
| `dry_setting.dry_duration` | int | Configured dry duration (`-1` = n/a) |
| `dry_setting.dry_filament` | string | Filament type for drying |
| `dry_setting.dry_temperature` | int | Dry temperature (`-1` = n/a) |
| `tray[]` | array | Per-slot tray objects |

#### Per-tray `ams.ams[].tray[]` (and `vir_slot[]`)

| Field | Type | Meaning |
| --- | --- | --- |
| `id` | string | Slot id within unit (`"0"`–`"3"`) or virtual `"254"`/`"255"` |
| `state` | int | Tray bitmask — see [Tray `state`](#ams-tray-state) (Studio does **not** parse) |
| `tray_type` | string | Filament type (`PLA`, …); empty = unknown / cleared |
| `tray_info_idx` | string | Filament product id (e.g. `GFA00`) |
| `tray_id_name` | string | Product colour / SKU label when present (e.g. `"S05-C0"`) — **not** the AMS slot UI name (`A1`/`B2`). See below and [§8.15.8](08.15-filament.md#8158-ams-identity-remain-and-trayidname) |
| `tray_sub_brands` | string | Sub-brand string |
| `tray_color` | string | RGBA hex without `#` |
| `cols` | array\<string\> | Multi-colour RGBA list |
| `ctype` | int | Colour type code |
| `nozzle_temp_min` / `nozzle_temp_max` | string | Nozzle temp range °C |
| `bed_temp` / `bed_temp_type` | string | Bed temp hints |
| `drying_temp` / `drying_time` | string | Filament drying defaults |
| `tray_diameter` | string | Filament diameter (e.g. `"1.75"`) |
| `tray_weight` | string | Spool weight |
| `remain` | int | Remaining filament **percent** estimate (`0`–`100`; `-1` = unknown). Filament Manager converts this to grams — [§8.15.8](08.15-filament.md#8158-ams-identity-remain-and-trayidname) |
| `remain_g` | int | Remaining weight in grams when firmware/UI edited it (`-1` = not provided). Parsed into `DevAmsTray`; **not** the input to Filament Manager AMS→cloud remain push |
| `total_len` | int | Total filament length estimate (mm-class units) |
| `tag_uid` | string | NFC **chip** id (~16 hex) — which physical tag the reader saw. All-`0` without RFID. Studio: `DevAmsTray::tag_uid` |
| `tray_uuid` | string | Logical **spool** UUID (~32 hex), shared by both faces of an official RFID spool. All-`0` without RFID. Studio: `DevAmsTray::uuid`. Filament Manager matches this to cloud `RFID` |
| `xcam_info` | string | Opaque AI/filament info blob |
| `cali_idx` | int | Active PA profile index (modern `cali_version >= 0`); absent on empty trays |
| `k` / `n` | number | Legacy PA K/N when `cali_version` absent — [§12.8](12.08-pa-calibration.md) |

##### `tag_uid` vs `tray_uuid` (Filament Manager identity)

These two tray strings are **not** interchangeable. Evidence: Studio parse + Filament Manager match ([`DevFilaSystem.cpp`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/DeviceCore/DevFilaSystem.cpp#L774), [`#L802`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/DeviceCore/DevFilaSystem.cpp#L802); [`match_tray`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/fila_manager/wgtFilaManagerSync.cpp#L218-L234)).

| MQTT key | C++ member | Role |
| --- | --- | --- |
| `tag_uid` | `DevAmsTray::tag_uid` | Hardware NFC chip id (**differs** if the opposite face is read) |
| `tray_uuid` | `DevAmsTray::uuid` | Logical spool id (**same** for both chips / faces) |

**Hardware context.** Official Bambu spools have **two NFC tags** (one per face). AMS hardware typically has **two readers for four slots**, so orientation determines which chip is scanned. Different `tag_uid`, one spool → firmware keeps a shared `tray_uuid`. Studio never spells out “two chips”, but Filament Manager keys inventory on `tray_uuid` so a flip does not fork the catalogue row. Details / `"01"` official-chip gate: [§8.15.8](08.15-filament.md#8158-ams-identity-remain-and-trayidname).

Filament Manager’s cloud field **`RFID`** / local spool field **`tag_uid`** stores the value that corresponds to **`tray_uuid`**, not the NFC `tag_uid`. AMS→catalogue match is:

`tray.uuid` (`tray_uuid`) **equals** catalogue `RFID` / `spool.tag_uid`.

The NFC chip field is **not** used in that comparison. Full remain sync, validity rules, and `trayIdName` semantics: [§8.15.8](08.15-filament.md#8158-ams-identity-remain-and-trayidname).

##### AMS tray `state`

Path: `print.ams.ams[<unit>].tray[<slot>].state` (integer).

Bambu Studio does **not** parse this field — presence comes from **`print.ams.tray_exist_bits`** (`DevFilaSystemParser::ParseAmsTrayInfo()`); metadata from `tray_type`, `tray_info_idx`, `tray_color`, etc.

**Legacy encoding (values 0–3 only)**

Older captures and community docs ([API_AMS_FILAMENT.md](https://github.com/coelacant1/Bambu-Lab-Cloud-API/blob/main/API_AMS_FILAMENT.md)) list only **0–3**. These already look like a **2-bit mask** (`state & 0x03`), not a sequential enum:

| `state` | Bits | Community name | Tentative meaning |
|--------:|------|----------------|-------------------|
| **0** | `00` | Empty | No spool |
| **1** | `01` | Loading | Spool engaged; metadata not ready (exact semantics unclear) |
| **2** | `10` | Loaded | Metadata side set without full “ready” (rare; semantics unclear) |
| **3** | `11` | Ready | Spool present **and** metadata applied — both low bits set when everything works |

Bit **0** (`0x01`) — spool **physically present** / tray engaged.  
Bit **1** (`0x02`) — **metadata** side; should be **1** when the slot is fully configured (**3**).

**Extended encoding (newer firmware — observed P2S + AMS, May 2026)**

Firmware adds more OR-ed flags on top of the same low two bits:

| Bit | Mask | Role (tentative) |
|-----|------|------------------|
| **0** | `0x01` | Spool present / tray engaged (unchanged) |
| **1** | `0x02` | Metadata known or **cached in memory** (see below) |
| **2** | `0x04` | RFID / motion — **TODO** (see below) |
| **3** | `0x08` | **Steady** slot report — idle states are **`0x08 \| (state & 0x03)`** |
| **4** | `0x10` | RFID / motion — **TODO** (see below) |

Steady states with bit **3** set (successors to legacy **0–3**):

| `state` | Low bits | Meaning |
|--------:|----------|---------|
| **8** | `00` | Hard empty — no spool, no remembered profile |
| **9** | `01` | Spool present, **no** metadata — UI **?**; MQTT often only **`{"id","state":9}`** |
| **10** | `10` | No spool, **cached** metadata for next insert |
| **11** | `11` | Spool present + metadata — normal configured tray (check **`tray_type`** non-empty; after **Clear**, **`state`** may stay **11** with blank fields — see below) |

So **`8 = 0x08\|0`**, **`9 = 0x08\|1`**, **`10 = 0x08\|2`**, **`11 = 0x08\|3`**.

**Which encoding does this printer use?**

- **`state ≥ 4`** → extended scheme (bit **3** and/or motion bits in play).
- On extended firmware, raw **`1`**, **`2`**, **`3`** should **not** appear as steady values: idle reporting always sets bit **3**, so steady codes are **8–11**. Values **1–3** would only fit legacy firmware without the **`0x08`** layer.
- If you only ever see **8–11** (and transient **5** / **17** / **21** / **27** during RFID), treat the slot as extended.

**Bit 1 in the extended scheme**

Bit **1** describes **metadata state**, not spool presence:

- **`1` with bit `0` clear** → **`10`**: slot empty, but the printer **remembers** a profile (colour/type) for the next spool.
- **`0` with bit `0` set** → **`9`**: spool **is** in the slot, but metadata was **cleared or invalidated** — UI **?**. Example: inserting a **non-RFID / third-party** spool after a configured **official Bambu** one; the printer detects presence but knows it is no longer the same known profile.

**Clear / reset from the printer filament UI**

**Clear** does **not** drop **`state`** to **9** / **10** — observed **`state: 11`** (`0x08\|0x03`, both low bits set). **`tray_exist_bits`** stays set. But filament fields in MQTT are **zeroed or emptied**, e.g.:

```json
{
  "id": "0",
  "state": 11,
  "tray_type": "",
  "tray_info_idx": "",
  "tray_color": "00000000",
  "cols": ["00000000"],
  "nozzle_temp_min": "0",
  "nozzle_temp_max": "0",
  "remain": -1,
  "tag_uid": "0000000000000000",
  "tray_uuid": "00000000000000000000000000000000",
  "…"
}
```

So **`state` bit 1 can stay set while the profile in JSON is already cleared** — do not treat **`state: 11`** alone as “fully configured”.

**Practical “?” detection:** empty **`tray_type`** (and usually empty **`tray_info_idx`**) → UI **?** / unknown spool, same as steady **`9`**, even when **`state`** still reports **11**. Do **not** use `tag_uid` or `tray_uuid` all-zero for this — those are normal without RFID.

Steady **9** with a spool installed typically sends only **`{"id":"N","state":9}`** — no `tray_type` / colour keys. **`state: 8`** can look the same shape but means an **empty** slot; use **`state`** (and **`tray_exist_bits`**) to tell them apart, not key count alone. **Clear** is different again: **full** tray object with blank/zero fields while **`state`** stays **11**.

**Bits 2 and 4 — RFID scan transients**

During **`ams_get_rfid`** ([§12.7](12.07-ams-filament.md)) with read-on-insert enabled, **`state`** flashes through non-steady values for sub-second to a few seconds. **P2S observed** chain: **`17 → 21 → 5 → 21 → 5`**, then steady **8 / 9 / 10 / 11**; a neighbouring slot may show **`27`**.

| `state` | Flags | Notes |
|--------:|-------|-------|
| **17** | `0x01\|0x10` | Brief prep phase |
| **21** | `0x01\|0x04\|0x10` | Longest phase — slow RFID scan |
| **5** | `0x01\|0x04` | Between **21** passes; **`21 ^ 5 = 0x10`** (only bit **4** toggles) |
| **27** | `0x08\|0x03\|0x10` | Parked neighbour during scan |

Bits **2** and **4** both change during RFID; **`tray_reading_bits`** / **`tray_read_done_bits`** in `print.ams` may correlate but were not fully mapped. **TODO:** confirm whether **`0x04`** vs **`0x10`** is feed vs tray rotation vs RFID channel; assignment may be swapped.

**Evidence for `tray.state`:** LAN MQTT on **P2S + AMS (May 2026)**; bitmask assignment unconfirmed in firmware sources.

Hub: [§12.1](12.01-status.md).
