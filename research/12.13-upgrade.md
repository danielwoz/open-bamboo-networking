### 12.13. Firmware upgrade

Studio-built `upgrade.*` MQTT commands. Plugin forwards only. Envelope: [§6.2](06.02-mqtt.md). Inbound `upgrade_state.*` / `force_upgrade` in `push_status`: [§12.1.6](12.01.06-fields-net-upgrade-errors.md).

#### `upgrade.upgrade_confirm`

Confirm a pending firmware upgrade.

| name | type/values | notes |
|---|---|---|
| `sequence_id` | string | Studio counter; example fixed to `"20000"` |
| `command` | string | constant `"upgrade_confirm"` |
| `src_id` | int | 1 = Studio |

**Reply parsing:** Generic upgrade error handling starts at `[`src/slic3r/GUI/DeviceManager.cpp:3645`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/DeviceManager.cpp#L3645)`; state at `DeviceCore/DevUpgrade.cpp:22-65`.

```json
{
  "upgrade": {
    "sequence_id": "20000",
    "command": "upgrade_confirm",
    "src_id": 1
  }
}
```

#### `upgrade.consistency_confirm`

Confirm firmware-version consistency.

| name | type/values | notes |
|---|---|---|
| `sequence_id` | string | Studio counter; example fixed to `"20000"` |
| `command` | string | constant `"consistency_confirm"` |
| `src_id` | int | 1 = Studio |

**Reply parsing:** Generic upgrade error handling starts at `[`src/slic3r/GUI/DeviceManager.cpp:3645`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/DeviceManager.cpp#L3645)`; state at `DeviceCore/DevUpgrade.cpp:22-65`.

```json
{
  "upgrade": {
    "sequence_id": "20000",
    "command": "consistency_confirm",
    "src_id": 1
  }
}
```

#### `upgrade.start`

Start a firmware/module upgrade from a URL.

| name | type/values | notes |
|---|---|---|
| `sequence_id` | string | Studio counter; example fixed to `"20000"` |
| `command` | string | constant `"start"` |
| `url` | string | firmware URL |
| `module` | string | target module |
| `version` | string | target version |
| `src_id` | int | 1 = Studio |

**Reply parsing:** Generic upgrade error handling starts at `[`src/slic3r/GUI/DeviceManager.cpp:3645`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/DeviceManager.cpp#L3645)`; state at `DeviceCore/DevUpgrade.cpp:22-65`.

```json
{
  "upgrade": {
    "sequence_id": "20000",
    "command": "start",
    "url": "https://example.invalid/firmware.sig",
    "module": "ota",
    "version": "01.00.00.00",
    "src_id": 1
  }
}
```

#### `upgrade.mc_for_ams_firmware_upgrade`

Switch the controller firmware profile used for AMS compatibility.

| name | type/values | notes |
|---|---|---|
| `sequence_id` | string | Studio counter; example fixed to `"20000"` |
| `command` | string | constant `"mc_for_ams_firmware_upgrade"` |
| `src_id` | int | 1 = Studio |
| `id` | int | firmware profile index |

**Reply parsing:** `DeviceManager.cpp:2775` → `[`src/slic3r/GUI/DeviceCore/DevFilaAmsSetting.cpp:22-66`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/DeviceCore/DevFilaAmsSetting.cpp#L22-L66)`.

```json
{
  "upgrade": {
    "sequence_id": "20000",
    "command": "mc_for_ams_firmware_upgrade",
    "src_id": 1,
    "id": 1
  }
}
```

#### `upgrade.wtm_upgrade`

Upgrade one nozzle WTM, all rack WTMs, or extruder WTM.

| name | type/values | notes |
|---|---|---|
| `sequence_id` | string | Studio counter; example fixed to `"20000"` |
| `command` | string | constant `"wtm_upgrade"` |
| `module` | string | `wtm`, `wtm/<rack-id>`, or `wtm_all` |
| `src_id` | int | 1 = Studio |

**Reply parsing:** Generic upgrade error handling starts at `[`src/slic3r/GUI/DeviceManager.cpp:3645`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/DeviceManager.cpp#L3645)`; state at `DeviceCore/DevUpgrade.cpp:22-65`.

```json
{
  "upgrade": {
    "sequence_id": "20000",
    "command": "wtm_upgrade",
    "module": "wtm_all",
    "src_id": 1
  }
}
```

Hub: [§12](12-mqtt.md).
