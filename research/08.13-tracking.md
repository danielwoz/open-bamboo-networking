### 8.13. Tracking / telemetry

Privacy-gated analytics surface: an opaque property store plus an event firehose. Studio drives it heavily after the user opts into improvement / privacy tracking; return codes are almost never checked. A compatible plugin that returns `0` from every symbol (and clears `track_get_property` to empty / `"0"`) is functionally indistinguishable for Studio’s own UI paths — stock additionally batches encrypted uploads to the cloud.

Studio resolves the exports via `dlsym` ([NetworkAgent.cpp:380-385](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/NetworkAgent.cpp#L380-L385)). Typedefs: [NetworkAgent.hpp:105-110](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/NetworkAgent.hpp#L105-L110). No track-specific `BAMBU_NETWORK_ERR_*` codes.

Neighbors: privacy consent HTTP ([§8.10.11](08.10-http.md#81011-bambu_network_report_consent)); HTTP hosts ([§6.6](06.06-cloud-rest.md)). **Not** this ABI: WebView GA / Clarity / `event.bblmw.com` / Sentry `perf.bblmw.com`; Studio log encryption also calls `GET …/analysis-st/tag/` ([GUI_App.cpp:2495-2507](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L2495-L2507)) outside the track symbols.


| Symbol | Signature | Notes |
| --- | --- | --- |
| `bambu_network_track_enable` | `int(void*, bool enable)` | Local + plugin gate |
| `bambu_network_track_header` | `int(void*, std::string header)` | Session metadata JSON |
| `bambu_network_track_remove_files` | `int(void*)` | Opt-out wipe |
| `bambu_network_track_update_property` | `int(void*, std::string name, std::string value, std::string type)` | Opaque KV write |
| `bambu_network_track_get_property` | `int(void*, std::string name, std::string& value, std::string type)` | Opaque KV read |
| `bambu_network_track_event` | `int(void*, std::string evt_key, std::string content)` | Event firehose |


#### 8.13.1. Common contract

**Consent gate.** Studio only enables tracking when `app_config["firstguide"]["privacyuse"] == "true"` ([`GUI_App::check_track_enable`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L5388-L5415)). Called after agent bring-up when already logged in, and again from `on_user_login`. Opt-out in Preferences clears the flag and calls `track_enable(false)` + `track_remove_files` ([Preferences.cpp:831-839](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/Preferences.cpp#L831-L839)); re-enable goes back through `check_track_enable` (not a bare `track_enable(true)` from Preferences).

**NetworkAgent wrapper gates** ([NetworkAgent.cpp:1635-1712](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/NetworkAgent.cpp#L1635-L1712)):

| Symbol | Called when |
| --- | --- |
| `track_enable` | Always (also sets local `enable_track`) |
| `track_remove_files` | Always (no `enable_track` check) |
| `track_header` / `track_update_property` / `track_get_property` | Only if `enable_track` |
| `track_event` | Only if `enable_track` **and** `is_user_login()` |

**Lifecycle.**

```text
privacy on + logged in:
  track_enable(true) → track_header({ver,os,name,uuid}) → track_event("studio_launch", …)
  … update_property / track_event during session …
  shutdown: track_get_property × N → aggregate track_event(key_func|…) → track_enable(false)

privacy off:
  track_remove_files()   (check_track_enable else-branch)
  or track_enable(false) + track_remove_files() (Preferences toggle)
```

Studio also keeps a local privacy-history file under `data_dir()/track/history.txt` ([GUI_App.cpp:5327-5333](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L5327-L5333)) — separate from the plugin’s buffered event files.

**Stock upload wire** (MITM stock `02.08.01.53`, cold start `stock_track_cold.mitm` / earlier hybrid dumps; UA `bambu_network_agent/…`):

1. `GET /v1/analysis-st/tag/?UID=<slicer_or_user_uid>` (Bearer) → JSON `{ "tag", "secret", "iv" }` (AES material; occasionally HTTP 400 `"resource is out"` then retry).
2. `GET /v1/iot-service/api/user/upload?event_track/slicer=<filename>.zip.enc` (Bearer) → presigned S3 URL; `urls[].type = "event_track/slicer"`.
3. `PUT <presigned S3 URL>` — body is encrypted (not a raw ZIP; observed ~0.5–1 KiB payloads, no `Authorization` header on the PUT).

Filename pattern observed:

```text
<user_id>_track_<plugin_ver>_<yyyymmddHHMMSS>_<tag>.zip.enc
```

Uploads are **batched** (not one HTTPS call per `track_event`).

**TODO**: need unpack of `.zip.enc` (how `secret`/`iv`/`tag` fold into ciphertext) and whether `track_header` fields appear inside the blob.

**Return convention.** `int`; Studio expects `0`. No dedicated track error family in [§8.16](08.16-errors.md).


#### 8.13.2. `bambu_network_track_enable`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `enable` | `bool` | `true` to arm tracking; `false` to disarm. |

**Purpose.** Turn the plugin’s tracking subsystem on or off; Studio also mirrors the flag in `NetworkAgent::enable_track`.

**Studio.** [`check_track_enable`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L5400) (`true`); Preferences privacy off ([Preferences.cpp:837](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/Preferences.cpp#L837)); MainFrame shutdown ([MainFrame.cpp:1127](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/MainFrame.cpp#L1127)) (`false`). Wrapper: [NetworkAgent.cpp:1635-1644](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/NetworkAgent.cpp#L1635-L1644).

**Plugin behaviour.** Persist enable state. When `false`, stop accepting new events / properties for upload (Studio already gates most calls). No dedicated HTTPS observed for enable itself — cloud activity follows later `track_event` / flush. Evidence: Studio source + MITM (upload only after enabled session).

**Return.** `0` on success.


#### 8.13.3. `bambu_network_track_header`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `header` | `std::string` | JSON object from Studio. |

**Purpose.** Attach session / client metadata to subsequent tracking payloads.

**Studio.** Only from [`check_track_enable`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L5392-L5401):

```json
{"ver":"<SLIC3R_VERSION>","os":"<wxGetOsDescription>","name":"<SLIC3R_APP_NAME>","uuid":"<slicer_uuid>"}
```

Wrapper: [NetworkAgent.cpp:1674-1684](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/NetworkAgent.cpp#L1674-L1684) (requires `enable_track`).

**Plugin behaviour.** Store header for inclusion in the next batched upload. No standalone HTTPS for this call. Evidence: Studio source.

**TODO**: confirm header fields appear inside the uploaded `.zip.enc` blob.

**Return.** `0` on success.


#### 8.13.4. `bambu_network_track_remove_files`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |

**Purpose.** Discard locally buffered track artifacts when the user opts out.

**Studio.** [`check_track_enable`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L5411-L5414) else-branch; Preferences privacy off ([Preferences.cpp:838](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/Preferences.cpp#L838)). Wrapper: [NetworkAgent.cpp:1647-1654](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/NetworkAgent.cpp#L1647-L1654).

**Plugin behaviour.** Delete plugin-local track buffers / pending upload files. No cloud `DELETE` observed in MITM dumps for this path. Evidence: Studio source; MITM (no matching DELETE).

**TODO**: need confirmation of on-disk paths stock clears under `remove_files`.

**Return.** `0` on success.


#### 8.13.5. `bambu_network_track_update_property`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `name` | `std::string` | Property key. |
| `value` | `std::string` | Property value (usually a decimal counter string; sometimes JSON). |
| `type` | `std::string` | Studio default `"string"` ([NetworkAgent.hpp:236](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/NetworkAgent.hpp#L236)). |

**Purpose.** Write an opaque key/value into the plugin’s property store (counters, feature usage, OTA versions, …).

**Studio.** ~25 call sites — e.g. `GLCanvas3D` / gizmos / menus (`auto_orient`, `auto_arrange`, `*_duration`, `*_menu`), Monitor tabs (`status` / `sd_card` / `HMS` / `update`), `select_device_page`, `ssdp_version`, `dev_ota_version` / `dev_ota_ver:<dev_id>`, Plater settings dump. Wrapper: [NetworkAgent.cpp:1687-1698](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/NetworkAgent.cpp#L1687-L1698).

**Plugin behaviour.** Upsert `name` → `value` in local store (include in later batched upload as stock does). No per-call HTTPS. Evidence: Studio source.

**Return.** `0` on success.


#### 8.13.6. `bambu_network_track_get_property`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `name` | `std::string` | Property key. |
| `value` | `std::string&` | out: stored value. |
| `type` | `std::string` | Studio default `"string"`. |

**Purpose.** Read back a previously stored property (mainly for shutdown aggregation).

**Studio.** Primary consumer: MainFrame close flush ([MainFrame.cpp:551-628](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/MainFrame.cpp#L551-L628)) — many `get_property` calls; empty string coerced to `"0"`, then emitted as `track_event("key_func"|"key_func_duration"|"menu_click"|"device_ctrl", …)`. Also duration counters in Monitor / GLToolbar / gizmos / AssemblySteps. Wrapper: [NetworkAgent.cpp:1701-1712](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/NetworkAgent.cpp#L1701-L1712).

**Plugin behaviour.** Return the stored string for `name`, or empty if missing. No HTTPS. Evidence: Studio source.

**Return.** `0` on success.


#### 8.13.7. `bambu_network_track_event`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `evt_key` | `std::string` | Event name. |
| `content` | `std::string` | Usually a JSON object string (`j.dump()`). |

**Purpose.** Record a named analytics event with a JSON payload.

**Studio.** Densest call surface (~69 sites). Gated by `enable_track && is_user_login()` in the wrapper. LiveView also routes through [`LiveViewTrackContext::sink_emit`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/Printer/LiveViewTrackContext.cpp#L44-L51) → `track_event(event_name, envelope_json)`. Wrapper: [NetworkAgent.cpp:1657-1671](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/NetworkAgent.cpp#L1657-L1671).

**Plugin behaviour.** Append `(evt_key, content)` to the local buffer; flush later via the Common upload sequence (`analysis-st` → `/user/upload?event_track/slicer=` → S3 PUT). Evidence: MITM `stock_track_cold.mitm` (batched PUT after cold start) + Studio source.

Literal `evt_key` values observed in Studio source (pin `12f17b06`; count of call sites with that string):

| `evt_key` | ~sites | Typical trigger |
| --- | ---: | --- |
| `third_cali` | 8 | Third-party / UI calibration |
| `cali` | 6 | Device / PA calibration dialogs |
| `networkplugin_download` | 5 | Plugin OTA progress |
| `printer_control` | 4 | Device axis / fan / control |
| `helio_state` | 4 | Helio feature state |
| `connect_dev` | 4 | Monitor / calibration connect |
| `enter_model_mall` | 3 | MakerWorld entry |
| `start_liveview` / `stop_liveview` | 2+1 | MediaPlayCtrl |
| `download_video` / `download_video_conn` | 2+2 | MediaFilePanel |
| `slice_group_mode` | 2 | Plater |
| `studio_launch` | 1 | `check_track_enable` |
| `privacy_policy` | 1 | Privacy dialog |
| `mqtt_active` / `mqtt_inactive` | 1+1 | Cloud MQTT up/down |
| `user_guide` / `preferences_changed` | 1+1 | Onboarding / prefs |
| `user_start_print` / `slice_completed` / `slice_mesh_stat` | 1 each | Print / slice |
| `save_project` / `import_file_type` / `printer_export_gcode` / `third_party_printer_job` | 1 each | Project I/O |
| `key_func` / `key_func_duration` / `menu_click` / `device_ctrl` | 1 each | Shutdown flush aggregates |
| `workflow_debug` / `message_delay` / `cmd_gcode_line` / `ack_cmd_*` / `assembly_view_export` / `dayil_tips` | 1 each | Misc |
| `studio_crash` | — | Present in source but inside a **commented** block — not live |

**LiveView dynamic keys** (via `LiveViewTrackContext` / `wxMediaCtrl3` player callbacks): `liveview_session_start`, `liveview_first_frame`, `liveview_session_end`, plus player `event->event_name` strings.

**Return.** `0` on success (or early no-op when gates fail — still `0`).

Hub: [§8](08-abi-main.md).
